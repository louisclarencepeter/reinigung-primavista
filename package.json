{
  "name": "reinigung-primavista",
  "private": true,
  "version": "1.0.0",
  "description": "Prima Vista — Bio Reinigung (React + Netlify Functions + MongoDB)",
  "scripts": {
    "install:all": "npm install && npm install --prefix client",
    "dev": "netlify dev",
    "build": "npm run build --prefix client"
  },
  "dependencies": {
    "mongoose": "^8.12.1"
  }
}
